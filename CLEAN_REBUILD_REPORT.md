# Rebuild record

The inherited v4.1 archive documented an earlier clean-directory rebuild. For the fresh v5.0 primary numerical regeneration and its precise scope, see `REPRODUCIBILITY_REPORT.md` and `benchmark_release/logs/pipeline_full.json`.
